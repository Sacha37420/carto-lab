# ── Django + API ─────────────────────────────────────────────────────────────
Django>=5.0,<6.0
djangorestframework>=3.15
drf-spectacular[sidecar]>=0.26
python-decouple>=3.8
psycopg2-binary>=2.9
django-cors-headers>=3.14.0

# ── SSO / JWT ────────────────────────────────────────────────────────────────
PyJWT>=2.8
cryptography>=40.0

# ── Stack SIG (GeoDjango s'appuie sur libgdal/libgeos/libproj système) ─────────
# Le binding Python GDAL est installé séparément dans le Dockerfile, à la version
# exacte de la lib système (pip install GDAL==$(gdal-config --version)).
shapely>=2.0
pyproj>=3.6
fiona>=1.9
geopandas>=1.0
rasterio>=1.3
numpy>=1.26
scipy>=1.11
